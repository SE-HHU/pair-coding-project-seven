package service;

import tool.Arithmetic;

import java.util.HashSet;

public class Database {
    public static HashSet<Arithmetic> hashSet = new HashSet<>();

    // public static boolean isRight(Arithmetic arithmetic, int naturalNumberMax,
    //                               int denominatorMax) {
    //     if (arithmetic.result.naturalNumber < 0 ||
    //             arithmetic.result.naturalNumber > naturalNumberMax) {
    //         return false;
    //     }
    //     for (int i = 0; i < arithmetic.operands.size(); i++) {
    //         if (arithmetic.operands.get(i).denominator < 0 ||
    //                 arithmetic.operands.get(i).denominator > denominatorMax) {
    //
    //         }
    //     }
    //
    //     return true;
    // }
}
